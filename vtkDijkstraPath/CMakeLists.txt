CMAKE_MINIMUM_REQUIRED(VERSION 2.8)
PROJECT(vtkDijkstraPath)
FIND_PACKAGE(VTK REQUIRED)
INCLUDE(${VTK_USE_FILE})
ADD_EXECUTABLE(vtkDijkstraPath vtkDijkstraPath.cpp)
TARGET_LINK_LIBRARIES(vtkDijkstraPath ${VTK_LIBRARIES})