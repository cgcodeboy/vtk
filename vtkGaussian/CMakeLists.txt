CMAKE_MINIMUM_REQUIRED(VERSION 2.8)
PROJECT(vtkGaussian)
FIND_PACKAGE(VTK REQUIRED)
INCLUDE(${VTK_USE_FILE})
ADD_EXECUTABLE(vtkGaussian vtkGaussian.cpp)
TARGET_LINK_LIBRARIES(vtkGaussian ${VTK_LIBRARIES})