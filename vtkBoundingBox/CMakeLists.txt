CMAKE_MINIMUM_REQUIRED(VERSION 2.8)
PROJECT(vtkBoundingBox)
FIND_PACKAGE(VTK REQUIRED)
INCLUDE(${VTK_USE_FILE})
ADD_EXECUTABLE(vtkBoundingBox vtkBoundingBox.CPP)
TARGET_LINK_LIBRARIES(vtkBoundingBox ${VTK_LIBRARIES})